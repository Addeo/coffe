# 🚨 Исправление: Сайт не загружается

## ❌ Проблема

Сайты **https://coffe-ug.ru** и **http://coffe-ug.ru** не работают после отключения Cloudflare Tunnel.

**Причина:**  
Раньше Cloudflare Tunnel проксировал трафик на внутренний порт 4000. Теперь трафик идет напрямую на IP сервера, но сервер не слушает на стандартных портах 80/443.

**Текущая ситуация:**

```
Internet (Port 80/443) ❌ → Ничего не слушает
Docker работает:       ✅ → Frontend: localhost:4000
                      ✅ → Backend: localhost:3001
```

## ✅ Решение

Установить **Nginx** на хост-машине как **reverse proxy**, который будет принимать трафик на портах 80/443 и перенаправлять на Docker-контейнеры.

---

## 🚀 БЫСТРОЕ ИСПРАВЛЕНИЕ (5 минут)

### 1️⃣ Загрузить файлы на сервер

```bash
# С вашего локального компьютера
cd /Users/sergejkosilov/WebstormProjects/new\ goal/coffe

scp nginx-host.conf setup-nginx-ssl.sh root@YOUR_SERVER_IP:/root/coffe/
```

### 2️⃣ Подключиться к серверу

```bash
ssh root@YOUR_SERVER_IP
cd /root/coffe
```

### 3️⃣ Запустить автоматическую установку

```bash
chmod +x setup-nginx-ssl.sh
sudo ./setup-nginx-ssl.sh
```

**Скрипт автоматически:**

- ✅ Установит Nginx и Certbot
- ✅ Получит SSL-сертификат от Let's Encrypt
- ✅ Настроит reverse proxy (80/443 → 4000/3001)
- ✅ Настроит автообновление сертификатов
- ✅ Запустит и проверит Nginx

### 4️⃣ Убедиться, что Docker работает

```bash
docker-compose -f docker-compose.prod.yml up -d
docker ps
```

Должны быть запущены:

- `coffee_mysql_prod`
- `coffee_backend_prod`
- `coffee_frontend_prod`

### 5️⃣ Проверить работу

```bash
# Автоматическая проверка
./check-site-health.sh

# Или вручную
curl -I https://coffe-ug.ru
```

**Открыть в браузере:**  
👉 **https://coffe-ug.ru** - должно работать! 🎉

---

## 📋 Что делает решение

**После установки:**

```
Internet (Port 80/443)
    ↓
  Nginx (Host Server) ✅
  ├→ https://coffe-ug.ru/     → Frontend (localhost:4000)
  ├→ https://coffe-ug.ru/api/ → Backend (localhost:3001)
  └→ SSL/TLS, Security Headers
```

**Возможности:**

- ✅ Автоматический редирект HTTP → HTTPS
- ✅ SSL/TLS сертификат от Let's Encrypt
- ✅ Автообновление сертификатов (каждые 12 часов)
- ✅ Безопасные заголовки (X-Frame-Options, CSP, etc.)
- ✅ Поддержка HTTP/2

---

## 🩺 Диагностика

### Проверка здоровья (автоматическая)

```bash
./check-site-health.sh
```

### Ручная проверка

```bash
# 1. Nginx работает?
systemctl status nginx

# 2. Порты открыты?
netstat -tulpn | grep -E '(80|443)'

# 3. Docker контейнеры запущены?
docker ps

# 4. Backend отвечает?
curl http://localhost:3001/api/test

# 5. Frontend отвечает?
curl http://localhost:4000

# 6. Сайт доступен из интернета?
curl -I https://coffe-ug.ru
```

### Логи

```bash
# Nginx
tail -f /var/log/nginx/coffe-ug.ru-access.log
tail -f /var/log/nginx/coffe-ug.ru-error.log

# Docker
docker logs -f coffee_frontend_prod
docker logs -f coffee_backend_prod
```

---

## 🔧 Типичные проблемы

### ❌ "502 Bad Gateway"

**Причина:** Docker-контейнеры не запущены

**Решение:**

```bash
docker-compose -f docker-compose.prod.yml up -d
docker ps
docker logs coffee_backend_prod
```

### ❌ "Connection refused"

**Причина:** Порт занят или firewall блокирует

**Решение:**

```bash
# Проверить, что занимает порты
netstat -tulpn | grep -E '(80|443)'

# Открыть порты в firewall
ufw allow 80/tcp
ufw allow 443/tcp
ufw status
```

### ❌ DNS не настроен

**Причина:** Домен не указывает на IP сервера

**Решение:**

```bash
# Проверить DNS
dig coffe-ug.ru
nslookup coffe-ug.ru

# Убедиться, что A-запись указывает на IP сервера
```

### ❌ "Certificate error"

**Причина:** SSL-сертификат не получен

**Решение:**

```bash
# Проверить сертификаты
certbot certificates

# Получить сертификат вручную
certbot certonly --webroot -w /var/www/certbot -d coffe-ug.ru -d www.coffe-ug.ru

# Или переустановить
./setup-nginx-ssl.sh
```

---

## 📝 Полезные команды

```bash
# === Nginx ===
systemctl status nginx          # Статус
systemctl restart nginx         # Перезапуск
systemctl reload nginx          # Перезагрузка конфигурации
nginx -t                        # Проверка конфигурации

# === Docker ===
docker ps                                              # Запущенные контейнеры
docker-compose -f docker-compose.prod.yml up -d        # Запустить
docker-compose -f docker-compose.prod.yml restart      # Перезапустить
docker-compose -f docker-compose.prod.yml down         # Остановить

# === SSL ===
certbot certificates            # Список сертификатов
certbot renew                   # Обновить сертификаты
certbot renew --dry-run         # Тест обновления

# === Логи ===
tail -f /var/log/nginx/coffe-ug.ru-error.log
docker logs -f coffee_backend_prod
docker logs -f coffee_frontend_prod

# === Проверка ===
./check-site-health.sh          # Автоматическая проверка
curl -I https://coffe-ug.ru     # Тест HTTPS
```

---

## 📚 Подробная документация

| Файл                            | Описание                               |
| ------------------------------- | -------------------------------------- |
| **БЫСТРОЕ*РЕШЕНИЕ*ПРОБЛЕМЫ.md** | Краткое руководство (5 минут)          |
| **NGINX_SETUP_GUIDE.md**        | Полная документация по настройке Nginx |
| **DEPLOY_TO_SERVER.md**         | Полное руководство по развертыванию    |
| **nginx-host.conf**             | Конфигурация Nginx для хоста           |
| **setup-nginx-ssl.sh**          | Скрипт автоматической установки        |
| **check-site-health.sh**        | Скрипт проверки здоровья сайта         |

---

## 🔐 Безопасность

✅ **Уже настроено:**

- SSL/TLS (HTTPS) с Let's Encrypt
- Современные протоколы (TLSv1.2, TLSv1.3)
- Безопасные заголовки (X-Frame-Options, X-Content-Type-Options, X-XSS-Protection)
- Автоматическое обновление сертификатов

⚠️ **Рекомендации:**

- Изменить дефолтные пароли в `.env`
- Настроить fail2ban для защиты от brute-force
- Регулярно обновлять систему: `apt update && apt upgrade`
- Настроить автоматические бэкапы базы данных

---

## ⚡ Быстрая шпаргалка

```bash
# На локальном компьютере
cd /Users/sergejkosilov/WebstormProjects/new\ goal/coffe
scp nginx-host.conf setup-nginx-ssl.sh root@SERVER_IP:/root/coffe/

# На сервере
ssh root@SERVER_IP
cd /root/coffe
chmod +x setup-nginx-ssl.sh check-site-health.sh
sudo ./setup-nginx-ssl.sh              # Установить Nginx + SSL
docker-compose -f docker-compose.prod.yml up -d  # Запустить Docker
./check-site-health.sh                 # Проверить здоровье

# Открыть в браузере
# https://coffe-ug.ru
```

---

## ✨ Результат

После выполнения всех шагов:

- ✅ **https://coffe-ug.ru** - работает
- ✅ **http://coffe-ug.ru** - автоматически редиректит на HTTPS
- ✅ **API**: https://coffe-ug.ru/api/
- ✅ **SSL-сертификат**: действителен и автоматически обновляется
- ✅ **Безопасность**: современные протоколы и заголовки
- ✅ **Performance**: HTTP/2, сжатие, кэширование

---

**Время выполнения:** ~5-10 минут  
**Требования:** Root-доступ к серверу  
**Сложность:** Низкая (автоматический скрипт)

---

**💡 Совет:** Сохраните этот файл и используйте его как справочник для будущих развертываний!
