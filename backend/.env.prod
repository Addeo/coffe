# Production Environment Variables for SberCloud

# Application Settings
NODE_ENV=production
PORT=3000

# Database Settings (using local MySQL Docker container)
# DB_HOST=mysql (configured in docker-compose.prod.yml)
# DB_PORT=3306 (configured in docker-compose.prod.yml)
# DB_USERNAME=coffee_user (configured in docker-compose.prod.yml)
# DB_PASSWORD=coffee_password (configured in docker-compose.prod.yml)
# DB_DATABASE=coffee_admin (configured in docker-compose.prod.yml)
# DB_SSL=false (configured in docker-compose.prod.yml)

# JWT Secret (generate strong random string)
JWT_SECRET=3addedc35315b1fa038eed58ff52a654c02386f7aa108f29225f51ca41af3fe0

# Email Configuration (Optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# File Upload Settings
UPLOAD_DEST=./uploads
MAX_FILE_SIZE=52428800
