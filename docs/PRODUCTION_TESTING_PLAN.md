# План тестирования продакшн приложения (КМВ - Кавказские Минеральные Воды)

**Дата создания:** 2025-01-XX  
**Цель:** Заполнение данных в продакшене через API и проверка статистики для региона КМВ

**Регион:** Кавказские Минеральные Воды (КМВ)
**Города:** Пятигорск, Кисловодск, Ессентуки, Железноводск, Минеральные Воды

---

## 📋 Обзор

Этот план описывает последовательность API запросов для:

1. Заполнения продакшн базы данных тестовыми данными по региону КМВ
2. Проверки корректности работы статистики
3. Валидации расчетов и отчетов для организаций и инженеров КМВ

---

## ⚙️ Настройка

### Переменные окружения

```bash
# URL продакшн API
PROD_API_URL=https://your-production-api.com/api

# Или для локального тестирования
PROD_API_URL=http://localhost:3001/api
```

### Требования

- Доступ к продакшн API
- Учетные данные администратора
- HTTP клиент (curl, Postman, или скрипт)

---

## 🔐 Этап 1: Аутентификация

### 1.1 Инициализация админа (если требуется)

- **Endpoint:** `GET /auth/init-admin`
- **Метод:** GET
- **Аутентификация:** Не требуется
- **Описание:** Создает админ пользователя, если его еще нет

**Пример запроса:**

```bash
curl -X GET "${PROD_API_URL}/auth/init-admin"
```

**Ожидаемый результат:**

```json
{
  "email": "admin@example.com",
  "password": "admin123",
  "message": "Admin user initialized"
}
```

**Сохранить:** `adminEmail`, `adminPassword`

---

### 1.2 Логин администратора

- **Endpoint:** `POST /auth/login`
- **Метод:** POST
- **Body:**

```json
{
  "email": "admin@example.com",
  "password": "admin123"
}
```

**Пример запроса:**

```bash
curl -X POST "${PROD_API_URL}/auth/login" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@example.com",
    "password": "admin123"
  }'
```

**Ожидаемый результат:**

```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": 1,
    "email": "admin@example.com",
    "firstName": "Admin",
    "lastName": "User",
    "role": "ADMIN"
  }
}
```

**Сохранить:** `adminToken`

---

## 🏢 Этап 2: Создание организаций

### 2.1 Создать организацию #1

- **Endpoint:** `POST /organizations`
- **Метод:** POST
- **Аутентификация:** `Authorization: Bearer {adminToken}`
- **Роль:** ADMIN
- **Body:**

```json
{
  "name": "ООО ТехСервис КМВ",
  "baseRate": 800.0,
  "overtimeMultiplier": 1.5,
  "hasOvertime": true,
  "isActive": true
}
```

**Сохранить:** `organizationId1`

---

### 2.2 Создать организацию #2

- **Endpoint:** `POST /organizations`
- **Body:**

```json
{
  "name": "ИП Санаторий Пятигорск",
  "baseRate": 600.0,
  "overtimeMultiplier": 1.3,
  "hasOvertime": true,
  "isActive": true
}
```

**Сохранить:** `organizationId2`

---

### 2.3 Создать организацию #3

- **Endpoint:** `POST /organizations`
- **Body:**

```json
{
  "name": "ЗАО Курортное Обслуживание КМВ",
  "baseRate": 1000.0,
  "overtimeMultiplier": 1.6,
  "hasOvertime": true,
  "isActive": true
}
```

**Сохранить:** `organizationId3`

---

### 2.4 Получить список организаций (проверка)

- **Endpoint:** `GET /organizations`
- **Метод:** GET
- **Аутентификация:** `Authorization: Bearer {adminToken}`
- **Ожидаемый результат:** Список всех созданных организаций

---

## 👥 Этап 3: Создание пользователей

### 3.1 Создать инженера #1 (штатный, стандартная ставка)

- **Endpoint:** `POST /users`
- **Метод:** POST
- **Аутентификация:** `Authorization: Bearer {adminToken}`
- **Роль:** ADMIN
- **Body:**

```json
{
  "email": "engineer1@test.com",
  "password": "engineer123",
  "firstName": "Иван",
  "lastName": "Петров",
  "role": "USER",
  "engineerType": "STAFF",
  "baseRate": 500.0,
  "overtimeCoefficient": 1.6,
  "planHoursMonth": 160,
  "homeTerritoryFixedAmount": 200.0,
  "fixedSalary": 0,
  "fixedCarAmount": 0
}
```

**Описание:** Штатный инженер с плановыми часами 160/месяц, стандартным коэффициентом переработки 1.6, фиксированной суммой за домашнюю территорию.

**Сохранить:** `engineer1UserId`, `engineer1Id` (из ответа)

---

### 3.2 Создать инженера #2 (наемный, контрактный)

- **Endpoint:** `POST /users`
- **Body:**

```json
{
  "email": "engineer2@test.com",
  "password": "engineer123",
  "firstName": "Сергей",
  "lastName": "Сидоров",
  "role": "USER",
  "engineerType": "CONTRACT",
  "baseRate": 400.0,
  "overtimeCoefficient": 1.5,
  "planHoursMonth": 0,
  "homeTerritoryFixedAmount": 0,
  "fixedSalary": 0,
  "fixedCarAmount": 0
}
```

**Описание:** Наемный (контрактный) инженер без плановых часов, с пониженным коэффициентом переработки 1.5, без фиксированных сумм.

**Сохранить:** `engineer2UserId`, `engineer2Id`

---

### 3.3 Создать инженера #3 (штатный с фиксированной зарплатой)

- **Endpoint:** `POST /users`
- **Body:**

```json
{
  "email": "engineer3@test.com",
  "password": "engineer123",
  "firstName": "Алексей",
  "lastName": "Козлов",
  "role": "USER",
  "engineerType": "STAFF",
  "baseRate": 450.0,
  "overtimeCoefficient": 1.6,
  "planHoursMonth": 160,
  "homeTerritoryFixedAmount": 180.0,
  "fixedSalary": 50000.0,
  "fixedCarAmount": 5000.0
}
```

**Описание:** Штатный инженер с фиксированной зарплатой 50000 руб/месяц и фиксированной оплатой за автомобиль 5000 руб/месяц.

**Сохранить:** `engineer3UserId`, `engineer3Id`

---

### 3.4 Создать инженера #4 (штатный с высоким коэффициентом переработки)

- **Endpoint:** `POST /users`
- **Body:**

```json
{
  "email": "engineer4@test.com",
  "password": "engineer123",
  "firstName": "Дмитрий",
  "lastName": "Волков",
  "role": "USER",
  "engineerType": "STAFF",
  "baseRate": 600.0,
  "overtimeCoefficient": 2.0,
  "planHoursMonth": 160,
  "homeTerritoryFixedAmount": 250.0,
  "fixedSalary": 0,
  "fixedCarAmount": 0
}
```

**Описание:** Штатный инженер с повышенной базовой ставкой 600 руб/час и высоким коэффициентом переработки 2.0, увеличенной суммой за домашнюю территорию.

**Сохранить:** `engineer4UserId`, `engineer4Id`

---

### 3.5 Создать инженера #5 (наемный с минимальными параметрами)

- **Endpoint:** `POST /users`
- **Body:**

```json
{
  "email": "engineer5@test.com",
  "password": "engineer123",
  "firstName": "Николай",
  "lastName": "Орлов",
  "role": "USER",
  "engineerType": "CONTRACT",
  "baseRate": 350.0,
  "overtimeCoefficient": 1.3,
  "planHoursMonth": 0,
  "homeTerritoryFixedAmount": 0,
  "fixedSalary": 0,
  "fixedCarAmount": 0
}
```

**Описание:** Наемный инженер с минимальной базовой ставкой 350 руб/час и минимальным коэффициентом переработки 1.3.

**Сохранить:** `engineer5UserId`, `engineer5Id`

---

### 3.6 Создать менеджера

- **Endpoint:** `POST /users`
- **Body:**

```json
{
  "email": "manager@test.com",
  "password": "manager123",
  "firstName": "Мария",
  "lastName": "Смирнова",
  "role": "MANAGER"
}
```

**Сохранить:** `managerUserId`

---

### 3.7 Логин менеджера

- **Endpoint:** `POST /auth/login`
- **Body:**

```json
{
  "email": "manager@test.com",
  "password": "manager123"
}
```

**Сохранить:** `managerToken`

---

### 3.8 Логин инженеров (для принятия соглашений)

**Инженер #1 (штатный, стандартная ставка):**

- **Endpoint:** `POST /auth/login`
- **Body:**

```json
{
  "email": "engineer1@test.com",
  "password": "engineer123"
}
```

**Сохранить:** `engineer1Token`

**Инженер #2 (наемный, контрактный):**

- **Endpoint:** `POST /auth/login`
- **Body:**

```json
{
  "email": "engineer2@test.com",
  "password": "engineer123"
}
```

**Сохранить:** `engineer2Token`

**Инженер #3 (штатный с фиксированной зарплатой):**

- **Endpoint:** `POST /auth/login`
- **Body:**

```json
{
  "email": "engineer3@test.com",
  "password": "engineer123"
}
```

**Сохранить:** `engineer3Token`

**Инженер #4 (штатный с высоким коэффициентом):**

- **Endpoint:** `POST /auth/login`
- **Body:**

```json
{
  "email": "engineer4@test.com",
  "password": "engineer123"
}
```

**Сохранить:** `engineer4Token`

**Инженер #5 (наемный с минимальными параметрами):**

- **Endpoint:** `POST /auth/login`
- **Body:**

```json
{
  "email": "engineer5@test.com",
  "password": "engineer123"
}
```

**Сохранить:** `engineer5Token`

---

## ✅ Этап 4: Работа с соглашениями

### 4.1 Получить активные соглашения (для каждого инженера)

- **Endpoint:** `GET /agreements`
- **Метод:** GET
- **Аутентификация:** `Authorization: Bearer {engineerToken}`
- **Ожидаемый результат:** Список обязательных соглашений
- **Сохранить:** `agreementIds[]`

---

### 4.2 Принять соглашения (для каждого инженера)

- **Endpoint:** `POST /agreements/accept`
- **Метод:** POST
- **Аутентификация:** `Authorization: Bearer {engineerToken}`
- **Body:**

```json
{
  "agreementIds": [1, 2, 3]
}
```

**Выполнить для:** `engineer1Token`, `engineer2Token`, `engineer3Token`, `engineer4Token`, `engineer5Token`

---

## 📋 Этап 5: Создание заявок

### 5.1 Создать заявку #1 (менеджер)

- **Endpoint:** `POST /orders`
- **Метод:** POST
- **Аутентификация:** `Authorization: Bearer {managerToken}`
- **Роль:** MANAGER
- **Body:**

```json
{
  "organizationId": {organizationId1},
  "title": "Ремонт оборудования в санатории Пятигорск",
  "description": "Требуется диагностика и ремонт серверного оборудования в административном корпусе",
  "location": "Пятигорск, пр. Кирова, 28",
  "distanceKm": 5.0,
  "territoryType": "CITY",
  "source": "MANUAL",
  "plannedStartDate": "2025-01-15T09:00:00Z"
}
```

**Сохранить:** `orderId1`

---

### 5.2 Создать заявку #2

- **Endpoint:** `POST /orders`
- **Body:**

```json
{
  "organizationId": {organizationId2},
  "title": "Установка системы видеонаблюдения в санатории",
  "description": "Монтаж и настройка системы видеонаблюдения на территории санатория",
  "location": "Кисловодск, ул. Мира, 15",
  "distanceKm": 22.0,
  "territoryType": "CITY",
  "source": "MANUAL",
  "plannedStartDate": "2025-01-16T10:00:00Z"
}
```

**Сохранить:** `orderId2`

---

### 5.3 Создать заявку #3

- **Endpoint:** `POST /orders`
- **Body:**

```json
{
  "organizationId": {organizationId3},
  "title": "Обслуживание сетевого оборудования в курортной зоне",
  "description": "Плановое обслуживание и замена компонентов сетевого оборудования",
  "location": "Ессентуки, ул. Интернациональная, 10",
  "distanceKm": 12.0,
  "territoryType": "CITY",
  "source": "AUTOMATIC",
  "plannedStartDate": "2025-01-17T08:00:00Z"
}
```

**Сохранить:** `orderId3`

---

### 5.4 Создать заявку #4 (для второго инженера)

- **Endpoint:** `POST /orders`
- **Body:**

```json
{
  "organizationId": {organizationId1},
  "title": "Ремонт принтера в администрации",
  "description": "Замена картриджей и настройка принтера в административном отделе",
  "location": "Железноводск, ул. Ленина, 8",
  "distanceKm": 8.0,
  "territoryType": "CITY",
  "source": "MANUAL",
  "plannedStartDate": "2025-01-18T11:00:00Z"
}
```

**Сохранить:** `orderId4`

---

### 5.5 Создать заявку #5 (для третьего инженера)

- **Endpoint:** `POST /orders`
- **Body:**

```json
{
  "organizationId": {organizationId2},
  "title": "Настройка Wi-Fi сети в гостинице",
  "description": "Установка и настройка точек доступа Wi-Fi в гостиничном комплексе",
  "location": "Минеральные Воды, пр. Карла Маркса, 45",
  "distanceKm": 18.0,
  "territoryType": "CITY",
  "source": "MANUAL",
  "plannedStartDate": "2025-01-19T14:00:00Z"
}
```

**Сохранить:** `orderId5`

---

## 👷 Этап 6: Назначение инженеров

### 6.1 Назначить инженера #1 на заявку #1

- **Endpoint:** `POST /orders/{orderId1}/assign-engineer`
- **Метод:** POST
- **Аутентификация:** `Authorization: Bearer {managerToken}`
- **Роль:** MANAGER
- **Body:**

```json
{
  "engineerId": {engineer1Id},
  "isPrimary": true
}
```

---

### 6.2 Назначить инженера #1 на заявку #2

- **Endpoint:** `POST /orders/{orderId2}/assign-engineer`
- **Body:**

```json
{
  "engineerId": {engineer1Id},
  "isPrimary": true
}
```

---

### 6.3 Назначить инженера #2 на заявку #3

- **Endpoint:** `POST /orders/{orderId3}/assign-engineer`
- **Body:**

```json
{
  "engineerId": {engineer2Id},
  "isPrimary": true
}
```

---

### 6.4 Назначить инженера #2 на заявку #4

- **Endpoint:** `POST /orders/{orderId4}/assign-engineer`
- **Body:**

```json
{
  "engineerId": {engineer2Id},
  "isPrimary": true
}
```

---

### 6.5 Назначить инженера #3 на заявку #5

- **Endpoint:** `POST /orders/{orderId5}/assign-engineer`
- **Body:**

```json
{
  "engineerId": {engineer3Id},
  "isPrimary": true
}
```

---

### 6.6 Создать дополнительные заявки для инженеров #4 и #5

**Заявка #6 (для инженера #4):**

- **Endpoint:** `POST /orders`
- **Body:**

```json
{
  "organizationId": {organizationId3},
  "title": "Ремонт системы кондиционирования",
  "description": "Диагностика и ремонт системы кондиционирования в административном здании",
  "location": "Пятигорск, ул. Красноармейская, 12",
  "distanceKm": 6.0,
  "territoryType": "CITY",
  "source": "MANUAL",
  "plannedStartDate": "2025-01-20T10:00:00Z"
}
```

**Сохранить:** `orderId6`

**Заявка #7 (для инженера #5):**

- **Endpoint:** `POST /orders`
- **Body:**

```json
{
  "organizationId": {organizationId1},
  "title": "Настройка системы безопасности",
  "description": "Установка и настройка системы контроля доступа",
  "location": "Кисловодск, ул. Курортный бульвар, 5",
  "distanceKm": 20.0,
  "territoryType": "CITY",
  "source": "MANUAL",
  "plannedStartDate": "2025-01-21T11:00:00Z"
}
```

**Сохранить:** `orderId7`

---

### 6.7 Назначить инженера #4 на заявку #6

- **Endpoint:** `POST /orders/{orderId6}/assign-engineer`
- **Body:**

```json
{
  "engineerId": {engineer4Id},
  "isPrimary": true
}
```

---

### 6.8 Назначить инженера #5 на заявку #7

- **Endpoint:** `POST /orders/{orderId7}/assign-engineer`
- **Body:**

```json
{
  "engineerId": {engineer5Id},
  "isPrimary": true
}
```

---

## ✅ Этап 7: Принятие заявок инженерами

### 7.1 Инженер #1 принимает заявку #1

- **Endpoint:** `POST /orders/{orderId1}/accept`
- **Метод:** POST
- **Аутентификация:** `Authorization: Bearer {engineer1Token}`
- **Роль:** USER

---

### 7.2 Инженер #1 принимает заявку #2

- **Endpoint:** `POST /orders/{orderId2}/accept`
- **Аутентификация:** `Authorization: Bearer {engineer1Token}`

---

### 7.3 Инженер #2 принимает заявку #3

- **Endpoint:** `POST /orders/{orderId3}/accept`
- **Аутентификация:** `Authorization: Bearer {engineer2Token}`

---

### 7.4 Инженер #2 принимает заявку #4

- **Endpoint:** `POST /orders/{orderId4}/accept`
- **Аутентификация:** `Authorization: Bearer {engineer2Token}`

---

### 7.5 Инженер #3 принимает заявку #5

- **Endpoint:** `POST /orders/{orderId5}/accept`
- **Аутентификация:** `Authorization: Bearer {engineer3Token}`

---

### 7.6 Инженер #4 принимает заявку #6

- **Endpoint:** `POST /orders/{orderId6}/accept`
- **Аутентификация:** `Authorization: Bearer {engineer4Token}`

---

### 7.7 Инженер #5 принимает заявку #7

- **Endpoint:** `POST /orders/{orderId7}/accept`
- **Аутентификация:** `Authorization: Bearer {engineer5Token}`

---

## 📝 Этап 8: Создание рабочих сессий

### 8.1 Создать рабочую сессию для заявки #1

- **Endpoint:** `POST /orders/{orderId1}/work-sessions`
- **Метод:** POST
- **Аутентификация:** `Authorization: Bearer {engineer1Token}`
- **Body:**

```json
{
  "workDate": "2025-01-15",
  "regularHours": 8,
  "overtimeHours": 0,
  "carPayment": 500,
  "distanceKm": 5.0,
  "territoryType": "CITY",
  "notes": "Выполнена диагностика и ремонт серверного оборудования в санатории Пятигорск",
  "canBeInvoiced": true
}
```

**Сохранить:** `workSessionId1`

---

### 8.2 Создать рабочую сессию для заявки #2

- **Endpoint:** `POST /orders/{orderId2}/work-sessions`
- **Body:**

```json
{
  "workDate": "2025-01-16",
  "regularHours": 6,
  "overtimeHours": 2,
  "carPayment": 800,
  "distanceKm": 22.0,
  "territoryType": "CITY",
  "notes": "Установка и настройка системы видеонаблюдения в санатории Кисловодск",
  "canBeInvoiced": true
}
```

**Сохранить:** `workSessionId2`

---

### 8.3 Создать рабочую сессию для заявки #3

- **Endpoint:** `POST /orders/{orderId3}/work-sessions`
- **Аутентификация:** `Authorization: Bearer {engineer2Token}`
- **Body:**

```json
{
  "workDate": "2025-01-17",
  "regularHours": 4,
  "overtimeHours": 0,
  "carPayment": 300,
  "distanceKm": 12.0,
  "territoryType": "CITY",
  "notes": "Плановое обслуживание сетевого оборудования в курортной зоне Ессентуки",
  "canBeInvoiced": true
}
```

**Сохранить:** `workSessionId3`

---

### 8.4 Создать рабочую сессию для заявки #4

- **Endpoint:** `POST /orders/{orderId4}/work-sessions`
- **Аутентификация:** `Authorization: Bearer {engineer2Token}`
- **Body:**

```json
{
  "workDate": "2025-01-18",
  "regularHours": 3,
  "overtimeHours": 0,
  "carPayment": 400,
  "distanceKm": 8.0,
  "territoryType": "CITY",
  "notes": "Замена картриджей и настройка принтера в администрации Железноводск",
  "canBeInvoiced": true
}
```

**Сохранить:** `workSessionId4`

---

### 8.5 Создать рабочую сессию для заявки #5

- **Endpoint:** `POST /orders/{orderId5}/work-sessions`
- **Аутентификация:** `Authorization: Bearer {engineer3Token}`
- **Body:**

```json
{
  "workDate": "2025-01-19",
  "regularHours": 5,
  "overtimeHours": 1,
  "carPayment": 600,
  "distanceKm": 18.0,
  "territoryType": "CITY",
  "notes": "Установка и настройка точек доступа Wi-Fi в гостинице Минеральные Воды",
  "canBeInvoiced": true
}
```

**Сохранить:** `workSessionId5`

---

### 8.6 Создать рабочую сессию для заявки #6

- **Endpoint:** `POST /orders/{orderId6}/work-sessions`
- **Аутентификация:** `Authorization: Bearer {engineer4Token}`
- **Body:**

```json
{
  "workDate": "2025-01-20",
  "regularHours": 7,
  "overtimeHours": 1,
  "carPayment": 550,
  "distanceKm": 6.0,
  "territoryType": "CITY",
  "notes": "Диагностика и ремонт системы кондиционирования в административном здании Пятигорск",
  "canBeInvoiced": true
}
```

**Сохранить:** `workSessionId6`

---

### 8.7 Создать рабочую сессию для заявки #7

- **Endpoint:** `POST /orders/{orderId7}/work-sessions`
- **Аутентификация:** `Authorization: Bearer {engineer5Token}`
- **Body:**

```json
{
  "workDate": "2025-01-21",
  "regularHours": 4,
  "overtimeHours": 0,
  "carPayment": 450,
  "distanceKm": 20.0,
  "territoryType": "CITY",
  "notes": "Установка и настройка системы контроля доступа в Кисловодске",
  "canBeInvoiced": true
}
```

**Сохранить:** `workSessionId7`

---

## ✅ Этап 9: Завершение работы

### 9.1 Завершить работу над заявкой #1

- **Endpoint:** `POST /orders/{orderId1}/complete-work`
- **Метод:** POST
- **Аутентификация:** `Authorization: Bearer {engineer1Token}`
- **Роль:** USER
- **Body:**

```json
{
  "regularHours": 8,
  "overtimeHours": 0,
  "carPayment": 500,
  "distanceKm": 5.0,
  "territoryType": "CITY",
  "notes": "Работа завершена успешно. Ремонт оборудования в санатории Пятигорск выполнен",
  "isFullyCompleted": true
}
```

---

### 9.2 Завершить работу над заявкой #2

- **Endpoint:** `POST /orders/{orderId2}/complete-work`
- **Body:**

```json
{
  "regularHours": 6,
  "overtimeHours": 2,
  "carPayment": 800,
  "distanceKm": 22.0,
  "territoryType": "CITY",
  "notes": "Работа завершена. Система видеонаблюдения установлена в санатории Кисловодск",
  "isFullyCompleted": true
}
```

---

### 9.3 Завершить работу над заявкой #3

- **Endpoint:** `POST /orders/{orderId3}/complete-work`
- **Аутентификация:** `Authorization: Bearer {engineer2Token}`
- **Body:**

```json
{
  "regularHours": 4,
  "overtimeHours": 0,
  "carPayment": 300,
  "distanceKm": 12.0,
  "territoryType": "CITY",
  "notes": "Обслуживание завершено. Сетевое оборудование в курортной зоне Ессентуки обслужено",
  "isFullyCompleted": true
}
```

---

### 9.4 Завершить работу над заявкой #4

- **Endpoint:** `POST /orders/{orderId4}/complete-work`
- **Аутентификация:** `Authorization: Bearer {engineer2Token}`
- **Body:**

```json
{
  "regularHours": 3,
  "overtimeHours": 0,
  "carPayment": 400,
  "distanceKm": 8.0,
  "territoryType": "CITY",
  "notes": "Ремонт завершен. Принтер в администрации Железноводск отремонтирован",
  "isFullyCompleted": true
}
```

---

### 9.5 Завершить работу над заявкой #5

- **Endpoint:** `POST /orders/{orderId5}/complete-work`
- **Аутентификация:** `Authorization: Bearer {engineer3Token}`
- **Body:**

```json
{
  "regularHours": 5,
  "overtimeHours": 1,
  "carPayment": 600,
  "distanceKm": 18.0,
  "territoryType": "CITY",
  "notes": "Настройка завершена. Wi-Fi сеть в гостинице Минеральные Воды настроена",
  "isFullyCompleted": true
}
```

---

### 9.6 Завершить работу над заявкой #6

- **Endpoint:** `POST /orders/{orderId6}/complete-work`
- **Аутентификация:** `Authorization: Bearer {engineer4Token}`
- **Body:**

```json
{
  "regularHours": 7,
  "overtimeHours": 1,
  "carPayment": 550,
  "distanceKm": 6.0,
  "territoryType": "CITY",
  "notes": "Работа завершена. Система кондиционирования в административном здании Пятигорск отремонтирована",
  "isFullyCompleted": true
}
```

---

### 9.7 Завершить работу над заявкой #7

- **Endpoint:** `POST /orders/{orderId7}/complete-work`
- **Аутентификация:** `Authorization: Bearer {engineer5Token}`
- **Body:**

```json
{
  "regularHours": 4,
  "overtimeHours": 0,
  "carPayment": 450,
  "distanceKm": 20.0,
  "territoryType": "CITY",
  "notes": "Работа завершена. Система контроля доступа в Кисловодске установлена и настроена",
  "isFullyCompleted": true
}
```

---

### 9.8 Завершить заявки (менеджер)

**Завершить заявку #1:**

- **Endpoint:** `POST /orders/{orderId1}/complete`
- **Метод:** POST
- **Аутентификация:** `Authorization: Bearer {managerToken}`
- **Роль:** MANAGER

**Завершить заявку #2:**

- **Endpoint:** `POST /orders/{orderId2}/complete`

**Завершить заявку #3:**

- **Endpoint:** `POST /orders/{orderId3}/complete`

**Завершить заявку #4:**

- **Endpoint:** `POST /orders/{orderId4}/complete`

**Завершить заявку #5:**

- **Endpoint:** `POST /orders/{orderId5}/complete`

**Завершить заявку #6:**

- **Endpoint:** `POST /orders/{orderId6}/complete`

**Завершить заявку #7:**

- **Endpoint:** `POST /orders/{orderId7}/complete`

---

## 📊 Этап 10: Проверка статистики

### 10.1 Статистика заявок (общая)

- **Endpoint:** `GET /orders/stats`
- **Метод:** GET
- **Аутентификация:** `Authorization: Bearer {adminToken}` или `{managerToken}`
- **Ожидаемый результат:**

```json
{
  "total": 5,
  "waiting": 0,
  "assigned": 0,
  "processing": 0,
  "working": 0,
  "review": 0,
  "completed": 5,
  "paid_to_engineer": 0,
  "bySource": {
    "manual": 4,
    "automatic": 1,
    "email": 0,
    "api": 0
  },
  "paymentStats": {
    "totalCompleted": 5,
    "receivedFromOrganization": 0,
    "pendingFromOrganization": 5,
    "paidToEngineer": 0,
    "pendingToEngineer": 5
  }
}
```

**Проверить:**

- ✅ Общее количество заявок = 5
- ✅ Все заявки завершены (completed = 5)
- ✅ Статистика по источникам корректна
- ✅ Статистика оплат корректна

---

### 10.2 Статистика инженера #1 (детальная)

- **Endpoint:** `GET /statistics/engineer/detailed?year=2025&month=1`
- **Метод:** GET
- **Аутентификация:** `Authorization: Bearer {engineer1Token}`
- **Ожидаемый результат:** Детальная статистика инженера за январь 2025

**Проверить:**

- ✅ Количество заявок = 2
- ✅ Общее количество часов (regular + overtime)
- ✅ Сумма заработка
- ✅ Сумма автомобильных выплат
- ✅ Расчеты корректны

---

### 10.3 Статистика инженера #2 (детальная)

- **Endpoint:** `GET /statistics/engineer/detailed?year=2025&month=1`
- **Аутентификация:** `Authorization: Bearer {engineer2Token}`

**Проверить:**

- ✅ Количество заявок = 2
- ✅ Общее количество часов
- ✅ Сумма заработка
- ✅ Расчеты корректны

---

### 10.4 Статистика инженера #3 (детальная)

- **Endpoint:** `GET /statistics/engineer/detailed?year=2025&month=1`
- **Аутентификация:** `Authorization: Bearer {engineer3Token}`

**Проверить:**

- ✅ Количество заявок = 1
- ✅ Общее количество часов
- ✅ Сумма заработка (с учетом фиксированной зарплаты)
- ✅ Расчеты корректны

---

### 10.5 Месячная статистика (админ)

- **Endpoint:** `GET /statistics/monthly?year=2025&month=1`
- **Метод:** GET
- **Аутентификация:** `Authorization: Bearer {adminToken}`
- **Роль:** ADMIN или MANAGER
- **Ожидаемый результат:** Месячная статистика за январь 2025

**Проверить:**

- ✅ Общее количество заявок
- ✅ Общее количество часов (regular + overtime)
- ✅ Общая сумма заработка инженеров
- ✅ Общая сумма оплат от организаций
- ✅ Прибыль
- ✅ Статистика по инженерам

---

### 10.6 Комплексная статистика (админ)

- **Endpoint:** `GET /statistics/comprehensive?year=2025&month=1&includeTimeBased=true&includeFinancial=true&includeRankings=true&includeForecast=true`
- **Метод:** GET
- **Аутентификация:** `Authorization: Bearer {adminToken}`
- **Роль:** ADMIN
- **Ожидаемый результат:** Комплексная статистика

**Проверить:**

- ✅ Временные показатели (часы работы)
- ✅ Финансовые показатели (заработок, оплаты)
- ✅ Рейтинги инженеров
- ✅ Прогнозы (если включены)

---

### 10.7 Статистика инженеров для админа

- **Endpoint:** `GET /statistics/admin/engineers?year=2025&month=1`
- **Метод:** GET
- **Аутентификация:** `Authorization: Bearer {adminToken}`
- **Роль:** ADMIN
- **Ожидаемый результат:** Статистика по всем инженерам

**Проверить:**

- ✅ Список всех инженеров
- ✅ Статистика по каждому инженеру
- ✅ Сравнение показателей

---

### 10.8 Статистика задолженностей по оплатам

- **Endpoint:** `GET /statistics/payment-debts?year=2025&month=1`
- **Метод:** GET
- **Аутентификация:** `Authorization: Bearer {adminToken}`
- **Роль:** ADMIN или MANAGER
- **Ожидаемый результат:** Статистика задолженностей

**Проверить:**

- ✅ Задолженности организаций перед системой
- ✅ Задолженности системы перед инженерами
- ✅ Суммы корректны

---

### 10.9 Статус оплаты автомобильных отчислений

- **Endpoint:** `GET /statistics/car-payment-status?year=2025&month=1`
- **Метод:** GET
- **Аутентификация:** `Authorization: Bearer {adminToken}`
- **Роль:** ADMIN
- **Ожидаемый результат:** Статус оплаты автомобильных отчислений

**Проверить:**

- ✅ Список инженеров с автомобильными выплатами
- ✅ Статусы оплат
- ✅ Суммы корректны

---

### 10.10 Статистика заработка инженера #1

- **Endpoint:** `GET /statistics/earnings?months=12`
- **Метод:** GET
- **Аутентификация:** `Authorization: Bearer {engineer1Token}`
- **Ожидаемый результат:** Статистика заработка за последние 12 месяцев

**Проверить:**

- ✅ Данные за текущий месяц присутствуют
- ✅ График заработка корректен

---

### 10.11 Сравнение заработка инженера #1

- **Endpoint:** `GET /statistics/earnings/comparison`
- **Метод:** GET
- **Аутентификация:** `Authorization: Bearer {engineer1Token}`
- **Ожидаемый результат:** Сравнение заработка с предыдущими периодами

---

### 10.12 Рейтинг инженера #1

- **Endpoint:** `GET /statistics/earnings/rank?year=2025&month=1`
- **Метод:** GET
- **Аутентификация:** `Authorization: Bearer {engineer1Token}`
- **Ожидаемый результат:** Рейтинг инженера по заработку

---

## ✅ Критерии успешности тестирования

### Заполнение данных

1. ✅ Создано 3 организации КМВ
2. ✅ Создано 5 инженеров со всеми возможными опциями:
   - Инженер #1: Штатный (STAFF), стандартная ставка, плановые часы 160/месяц
   - Инженер #2: Наемный (CONTRACT), без плановых часов
   - Инженер #3: Штатный с фиксированной зарплатой и оплатой за автомобиль
   - Инженер #4: Штатный с высоким коэффициентом переработки (2.0)
   - Инженер #5: Наемный с минимальными параметрами
3. ✅ Создан 1 менеджер
4. ✅ Создано 7 заявок в городах КМВ
5. ✅ Все заявки назначены на инженеров
6. ✅ Все заявки приняты инженерами
7. ✅ Создано 7 рабочих сессий
8. ✅ Все заявки завершены

### Проверка статистики

1. ✅ Общая статистика заявок корректна
2. ✅ Статистика каждого инженера корректна
3. ✅ Месячная статистика корректна
4. ✅ Комплексная статистика корректна
5. ✅ Расчеты заработка корректны
6. ✅ Расчеты оплат от организаций корректны
7. ✅ Статистика задолженностей корректна
8. ✅ Статусы оплат корректны

---

## 📝 Примечания

### Форматы данных

- **Даты:** ISO 8601 формат (`YYYY-MM-DD` или `YYYY-MM-DDTHH:mm:ssZ`)
- **Суммы:** Decimal числа с 2 знаками после запятой
- **Токены:** Использовать с префиксом `Bearer ` в заголовке `Authorization`

### Обработка ошибок

- **401 Unauthorized:** Требуется повторная аутентификация
- **403 Forbidden:** Недостаточно прав (проверить роль пользователя)
- **404 Not Found:** Ресурс не найден (проверить ID)
- **400 Bad Request:** Неверные данные в запросе (проверить body)

### Рекомендации

1. Выполнять запросы последовательно, сохраняя ID для последующих запросов
2. Проверять ответы каждого запроса перед переходом к следующему
3. Логировать все запросы и ответы для отладки
4. Использовать переменные окружения для URL и токенов
5. Создавать скрипт для автоматизации выполнения плана

---

## 🔄 Последовательность выполнения (краткая)

1. Аутентификация → `adminToken`
2. Создание организаций → `organizationId1-3`
3. Создание пользователей → `engineer1-3Id`, `managerUserId`
4. Логин пользователей → `engineer1-3Token`, `managerToken`
5. Принятие соглашений инженерами
6. Создание заявок → `orderId1-5`
7. Назначение инженеров на заявки
8. Принятие заявок инженерами
9. Создание рабочих сессий → `workSessionId1-5`
10. Завершение работы над заявками
11. Завершение заявок менеджером
12. Проверка статистики (все эндпоинты)

---

## 📄 Пример скрипта для автоматизации

Создайте файл `scripts/test-production-api.js` для автоматизации выполнения плана.

```javascript
// Пример структуры скрипта
const API_URL = process.env.PROD_API_URL || 'https://your-production-api.com/api';

async function testProduction() {
  // 1. Аутентификация
  const adminToken = await loginAdmin();

  // 2. Создание организаций
  const orgIds = await createOrganizations(adminToken);

  // 3. Создание пользователей
  const userIds = await createUsers(adminToken);

  // ... и так далее
}

testProduction();
```

---

**Дата последнего обновления:** 2025-01-XX
