# Руководство по тестированию API

## Быстрый старт

### 1. Убедитесь, что backend запущен

```bash
cd backend
npm run start:dev
```

Backend должен быть доступен на `http://localhost:3001`

### 2. Запустите автоматическое тестирование

```bash
cd backend
npm run test:api
```

Или напрямую:

```bash
node scripts/test-api.js
```

### 3. Проверьте результаты

Скрипт выполнит полный цикл тестирования:
1. ✅ Инициализация админа
2. ✅ Логин админа
3. ✅ Создание организации
4. ✅ Создание инженера
5. ✅ Создание менеджера
6. ✅ Логин менеджера
7. ✅ Логин инженера
8. ✅ Принятие соглашений
9. ✅ Создание заявки
10. ✅ Назначение инженера
11. ✅ Принятие заявки инженером
12. ✅ Создание рабочей сессии
13. ✅ Завершение работы
14. ✅ Проверка статистики

## Настройка

### Переменные окружения

Вы можете изменить URL API через переменную окружения:

```bash
API_URL=http://localhost:3001/api npm run test:api
```

### Логи и вывод

Скрипт выводит:
- ✅ Зеленым - успешные тесты
- ❌ Красным - ошибки
- 🔵 Синим - информационные сообщения
- ⚠️ Желтым - предупреждения

## Требования

- Node.js 18+ (для поддержки fetch API)
- Запущенный backend сервер
- Доступ к базе данных

## Что тестируется

### Аутентификация
- ✅ Инициализация админа
- ✅ Логин админа
- ✅ Логин менеджера
- ✅ Логин инженера

### Соглашения
- ✅ Получение активных соглашений
- ✅ Принятие соглашений

### Организации
- ✅ Создание организации

### Пользователи
- ✅ Создание инженера
- ✅ Создание менеджера

### Заявки
- ✅ Создание заявки
- ✅ Получение заявки
- ✅ Назначение инженера
- ✅ Принятие заявки
- ✅ Завершение работы
- ✅ Завершение заявки

### Рабочие сессии
- ✅ Создание рабочей сессии
- ✅ Получение рабочих сессий

### Статистика
- ✅ Получение статистики инженера

## Ручное тестирование

Вы также можете использовать любой HTTP клиент (Postman, Insomnia, curl) для тестирования отдельных эндпоинтов.

См. `docs/API_TESTING_PLAN.md` для полного списка эндпоинтов и примеров запросов.

## Устранение неполадок

### Ошибка: "Cannot find module 'fetch'"

Убедитесь, что используете Node.js 18+:

```bash
node --version
```

Если версия ниже 18, обновите Node.js или установите node-fetch:

```bash
npm install node-fetch
```

И обновите скрипт для использования node-fetch.

### Ошибка: "Connection refused"

Убедитесь, что backend запущен:

```bash
cd backend
npm run start:dev
```

### Ошибка: "401 Unauthorized"

Проверьте, что админ был инициализирован:

```bash
curl http://localhost:3001/api/auth/init-admin
```

### Ошибка: "403 Forbidden"

Проверьте, что пользователь имеет правильную роль для выполнения операции.

