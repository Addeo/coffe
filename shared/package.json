{
  "name": "servicecheck-shared",
  "version": "1.1.5",
  "description": "Shared types and DTOs for ServiceCheck Panel",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
