# Развертывание на сервер (Production)

## 📦 Что нужно

1. ✅ Сервер с Ubuntu/Debian
2. ✅ Root доступ (SSH)
3. ✅ Домен coffe-ug.ru настроен (A-запись указывает на IP сервера)
4. ✅ Открыты порты: 22 (SSH), 80 (HTTP), 443 (HTTPS)

## 🚀 Быстрое развертывание (с нуля)

### Шаг 1: Подготовить сервер

```bash
# Подключиться к серверу
ssh root@your-server-ip

# Обновить систему
apt update && apt upgrade -y

# Установить Docker и Docker Compose
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Установить Docker Compose
apt install -y docker-compose

# Создать директорию для проекта
mkdir -p /root/coffe
cd /root/coffe
```

### Шаг 2: Загрузить проект на сервер

**Вариант А: Через Git**
```bash
# На сервере
cd /root/coffe
git clone https://github.com/your-username/coffe.git .

# Или обновить существующий
git pull
```

**Вариант Б: Через SCP (с локального компьютера)**
```bash
# С вашего компьютера
cd /Users/sergejkosilov/WebstormProjects/new\ goal/coffe

# Загрузить все файлы (может быть медленно)
rsync -avz --exclude 'node_modules' --exclude '.git' . root@your-server-ip:/root/coffe/

# Или только необходимые файлы
scp docker-compose.prod.yml nginx-host.conf setup-nginx-ssl.sh root@your-server-ip:/root/coffe/
```

### Шаг 3: Настроить окружение

```bash
# На сервере
cd /root/coffe

# Создать .env файл из примера
cp env.example .env

# Отредактировать .env (ВАЖНО!)
nano .env

# Установить следующие значения:
# NODE_ENV=production
# DB_HOST=mysql
# DB_PORT=3306
# DB_USERNAME=coffee_user
# DB_PASSWORD=СИЛЬНЫЙ_ПАРОЛЬ
# DB_DATABASE=coffee_admin
# JWT_SECRET=СГЕНЕРИРОВАННЫЙ_64_СИМВОЛЬНЫЙ_КЛЮЧ
# PORT=3001
```

**Генерация JWT_SECRET:**
```bash
node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
```

### Шаг 4: Запустить Docker-контейнеры

```bash
cd /root/coffe

# Собрать и запустить контейнеры
docker-compose -f docker-compose.prod.yml up -d --build

# Проверить статус
docker ps

# Должны быть запущены:
# - coffee_mysql_prod
# - coffee_backend_prod
# - coffee_frontend_prod

# Посмотреть логи
docker logs coffee_backend_prod
docker logs coffee_frontend_prod
```

### Шаг 5: Настроить Nginx и SSL

```bash
cd /root/coffe

# Запустить автоматическую установку
chmod +x setup-nginx-ssl.sh
./setup-nginx-ssl.sh

# Скрипт выполнит:
# ✅ Установку Nginx
# ✅ Получение SSL-сертификата
# ✅ Настройку reverse proxy
# ✅ Автообновление сертификатов
```

### Шаг 6: Проверить работу

```bash
# Запустить проверку здоровья
chmod +x check-site-health.sh
./check-site-health.sh

# Проверить в браузере
# https://coffe-ug.ru - должен загрузиться сайт
```

## 🔄 Обновление приложения

```bash
# Подключиться к серверу
ssh root@your-server-ip
cd /root/coffe

# Обновить код (Git)
git pull

# Или загрузить новую версию (SCP)
# scp docker-compose.prod.yml root@your-server-ip:/root/coffe/

# Пересобрать и перезапустить контейнеры
docker-compose -f docker-compose.prod.yml up -d --build

# Проверить
docker ps
docker logs -f coffee_backend_prod
```

## 🩺 Диагностика проблем

### Проверка здоровья

```bash
# Автоматическая проверка
./check-site-health.sh

# Ручные проверки
systemctl status nginx
docker ps
netstat -tulpn | grep -E '(80|443|3001|4000)'
```

### Логи

```bash
# Nginx логи
tail -f /var/log/nginx/coffe-ug.ru-access.log
tail -f /var/log/nginx/coffe-ug.ru-error.log

# Docker логи
docker logs -f coffee_frontend_prod
docker logs -f coffee_backend_prod
docker logs -f coffee_mysql_prod
```

### Перезапуск сервисов

```bash
# Перезапустить Nginx
systemctl restart nginx

# Перезапустить Docker-контейнеры
docker-compose -f docker-compose.prod.yml restart

# Полный перезапуск (с пересборкой)
docker-compose -f docker-compose.prod.yml down
docker-compose -f docker-compose.prod.yml up -d --build
```

## 🛠️ Типичные проблемы

### Проблема: "502 Bad Gateway"

**Причина:** Docker-контейнеры не запущены или недоступны

**Решение:**
```bash
docker ps  # Проверить статус
docker-compose -f docker-compose.prod.yml up -d  # Запустить
docker logs coffee_backend_prod  # Посмотреть логи
```

### Проблема: "Connection timed out"

**Причина:** Firewall блокирует порты

**Решение:**
```bash
# Открыть порты
ufw allow 80/tcp
ufw allow 443/tcp
ufw status
```

### Проблема: "SSL certificate error"

**Причина:** Сертификат не получен или истек

**Решение:**
```bash
# Проверить сертификат
certbot certificates

# Обновить сертификат
certbot renew

# Переустановить
./setup-nginx-ssl.sh
```

### Проблема: "Database connection error"

**Причина:** MySQL не запущен или неверные credentials

**Решение:**
```bash
# Проверить MySQL
docker logs coffee_mysql_prod

# Перезапустить
docker restart coffee_mysql_prod

# Проверить .env файл
cat .env | grep DB_
```

## 📊 Архитектура Production

```
┌─────────────────────────────────────────┐
│         Internet (Port 80/443)          │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│     Nginx (Host Server)                 │
│  - SSL/TLS Termination                  │
│  - Reverse Proxy                        │
│  - Security Headers                     │
└──────────┬──────────────────────────────┘
           │
     ┌─────┴──────┐
     │            │
     ▼            ▼
┌─────────┐  ┌─────────┐
│Frontend │  │Backend  │
│(Port    │  │(Port    │
│4000)    │  │3001)    │
│         │  │         │
│Angular  │  │NestJS   │
│+ Nginx  │  │API      │
└─────────┘  └────┬────┘
                  │
                  ▼
             ┌─────────┐
             │MySQL    │
             │Database │
             │(Port    │
             │3306)    │
             └─────────┘
```

## 🔐 Безопасность

✅ **Настроено:**
- SSL/TLS (HTTPS)
- Автообновление сертификатов
- Безопасные заголовки
- CORS настроен правильно

⚠️ **Рекомендации:**
- Изменить дефолтные пароли в `.env`
- Настроить fail2ban
- Регулярно обновлять систему
- Настроить автоматические бэкапы

## 📝 Полезные команды

```bash
# Статус всех сервисов
systemctl status nginx
docker ps

# Логи
docker logs -f coffee_backend_prod
tail -f /var/log/nginx/coffe-ug.ru-error.log

# Перезапуск
systemctl reload nginx
docker-compose -f docker-compose.prod.yml restart

# Обновление
git pull
docker-compose -f docker-compose.prod.yml up -d --build

# Очистка
docker system prune -a  # Удалить неиспользуемые образы
docker volume prune     # Удалить неиспользуемые volumes

# Бэкап базы данных
docker exec coffee_mysql_prod mysqldump -u coffee_user -p coffee_admin > backup.sql

# Восстановление базы данных
docker exec -i coffee_mysql_prod mysql -u coffee_user -p coffee_admin < backup.sql
```

## 📚 Дополнительные ресурсы

- **Полная инструкция:** `NGINX_SETUP_GUIDE.md`
- **Быстрое решение проблем:** `БЫСТРОЕ_РЕШЕНИЕ_ПРОБЛЕМЫ.md`
- **Проверка здоровья:** `./check-site-health.sh`

## 💬 Поддержка

Если возникают проблемы:
1. Запустить `./check-site-health.sh`
2. Проверить логи (nginx, docker)
3. Проверить конфигурацию `.env`
4. Убедиться, что DNS настроен правильно
5. Проверить firewall правила

---

**Время развертывания:** ~15-20 минут  
**Сложность:** Средняя  
**Требования:** Root доступ + базовые знания Linux

