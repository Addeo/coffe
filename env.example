# ===============================================
# ВАЖНО: Скопируйте этот файл в .env перед использованием!
# cp env.example .env
# ===============================================

# ===============================================
# NODE ENVIRONMENT
# ===============================================
NODE_ENV=development
# Для production используйте: NODE_ENV=production

# ===============================================
# DATABASE CONFIGURATION
# ===============================================
# Рекомендуется: MySQL везде (dev через Docker, prod на сервере)
# Запуск MySQL для dev: docker-compose -f docker-compose.dev.yml up -d

DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=coffee_user
DB_PASSWORD=CHANGE_THIS_PASSWORD
DB_DATABASE=coffee_admin
DB_SSL=false
# В production: DB_SSL=true

# ===============================================
# JWT CONFIGURATION - КРИТИЧНО!
# ===============================================
# ⚠️ ОБЯЗАТЕЛЬНО измените перед запуском!
# 
# Генерация секрета:
# node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
# Или: openssl rand -base64 64
#
# Минимум 32 символа, УНИКАЛЬНЫЙ для каждого окружения (dev/prod)!
JWT_SECRET=CHANGE_THIS_TO_RANDOM_64_CHARACTER_STRING_GENERATED_WITH_COMMAND_ABOVE

# JWT Token expiration
JWT_EXPIRES_IN=24h

# ===============================================
# APPLICATION
# ===============================================
PORT=3001

# File upload limits (в байтах, 10MB по умолчанию)
MAX_FILE_SIZE=10485760
UPLOAD_DEST=./uploads

# ===============================================
# FRONTEND URL (для CORS)
# ===============================================
FRONTEND_URL=http://localhost:4202
# Для production укажите ваш домен:
# FRONTEND_URL=https://yourdomain.com
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ===============================================
# API CONFIGURATION
# ===============================================
API_URL=http://localhost:3001/api

# Google Gmail Integration (опционально)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_REDIRECT_URI=http://localhost:3001/api/gmail/auth/callback
GOOGLE_ACCESS_TOKEN=
GOOGLE_REFRESH_TOKEN=

# SMTP Configuration (для email уведомлений)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Production Settings
# NODE_ENV=production
# PORT=3001
# UPLOAD_DEST=./uploads
# MAX_FILE_SIZE=52428800
