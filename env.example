# Database Configuration (для продакшена используйте PostgreSQL или MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=coffee_user
DB_PASSWORD=coffee_password
DB_DATABASE=coffee_admin

# Для SQLite (простой вариант для продакшена):
# DB_TYPE=sqlite
# DB_DATABASE=database.sqlite

# JWT Configuration (ОБЯЗАТЕЛЬНО измените на случайную строку!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-2025

# Application Configuration
NODE_ENV=development
PORT=3001

# Frontend Configuration
API_URL=http://localhost:3001/api

# Google Gmail Integration (опционально)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_REDIRECT_URI=http://localhost:3001/api/gmail/auth/callback
GOOGLE_ACCESS_TOKEN=
GOOGLE_REFRESH_TOKEN=

# SMTP Configuration (для email уведомлений)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Production Settings
# NODE_ENV=production
# PORT=3001
# UPLOAD_DEST=./uploads
# MAX_FILE_SIZE=52428800
