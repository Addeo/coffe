# Testing and Seeding Guide

Руководство по тестированию всех эндпоинтов и заполнению базы данных тестовыми данными.

## 📋 Содержание

1. [Заполнение тестовыми данными](#заполнение-тестовыми-данными)
2. [Тестирование эндпоинтов](#тестирование-эндпоинтов)
3. [Тестовые учетные записи](#тестовые-учетные-записи)

## 🌱 Заполнение тестовыми данными

### Базовый seed (минимальные данные)

Создает только базовых пользователей и организации:

```bash
cd backend
npm run seed
```

### Расширенный seed (полные тестовые данные)

Создает полный набор тестовых данных для тестирования всех функций системы:

```bash
cd backend
npm run seed:comprehensive
```

#### Что создается:

- **Пользователи:**
  - 1 администратор
  - 2 менеджера
  - 3 инженера

- **Организации:**
  - 6 организаций с разными тарифами

- **Инженеры:**
  - 3 инженера (штатные и наемные)

- **Заказы:**
  - 6 заказов с разными статусами (waiting, assigned, working, review, completed)

- **Рабочие сессии:**
  - Сессии для заказов в работе и завершенных

- **Тарифы инженер-организация:**
  - Индивидуальные тарифы для инженеров

- **Выплаты:**
  - Авансы и зарплаты для всех инженеров

- **Продукты:**
  - 4 тестовых продукта

- **Настройки:**
  - Базовые настройки системы

## 🧪 Тестирование эндпоинтов

### Комплексное тестирование всех эндпоинтов

Скрипт автоматически тестирует все эндпоинты API с разными ролями:

```bash
# Тестирование локального сервера
./test-all-endpoints-comprehensive.sh

# Тестирование удаленного сервера
./test-all-endpoints-comprehensive.sh http://192.144.12.102:3001/api
```

### Что тестируется:

#### Публичные эндпоинты:
- `GET /api/auth/init-admin` - Инициализация админа
- `GET /api/organizations/test` - Тестовый эндпоинт
- `GET /api/organizations/public` - Публичный список организаций
- `GET /api/test` - Тестовый эндпоинт

#### Эндпоинты с аутентификацией:

**Пользователи (ADMIN):**
- `GET /api/users` - Список пользователей
- `POST /api/users` - Создать пользователя
- `GET /api/users/:id` - Получить пользователя
- `PATCH /api/users/:id` - Обновить пользователя
- `GET /api/users/profile` - Свой профиль
- `PATCH /api/users/profile` - Обновить профиль

**Организации (ADMIN):**
- `GET /api/organizations` - Список организаций
- `POST /api/organizations` - Создать организацию
- `GET /api/organizations/:id` - Получить организацию
- `PATCH /api/organizations/:id` - Обновить организацию
- `PATCH /api/organizations/:id/toggle-status` - Переключить статус

**Заказы:**
- `GET /api/orders` - Список заказов
- `POST /api/orders` - Создать заказ
- `GET /api/orders/stats` - Статистика заказов
- `GET /api/orders/my-orders` - Мои заказы
- `GET /api/orders/by-source/:source` - Заказы по источнику
- `GET /api/orders/:id` - Получить заказ
- `PATCH /api/orders/:id` - Обновить заказ
- `GET /api/orders/:id/work-sessions` - Рабочие сессии заказа

**Статистика (ADMIN, MANAGER):**
- `GET /api/statistics` - Общая статистика
- `GET /api/statistics/organizations` - Статистика по организациям
- `GET /api/statistics/engineers` - Статистика по инженерам
- `GET /api/statistics/engineer/detailed` - Детальная статистика

**Выплаты (ADMIN, MANAGER):**
- `GET /api/salary-payments` - Список выплат

**Тарифы (ADMIN, MANAGER):**
- `GET /api/engineer-organization-rates` - Список тарифов

**Уведомления:**
- `GET /api/notifications` - Список уведомлений
- `GET /api/notifications/unread-count` - Количество непрочитанных

**Настройки (ADMIN, MANAGER):**
- `GET /api/settings` - Получить настройки

**Файлы:**
- `GET /api/files` - Список файлов

**Отчеты (ADMIN, MANAGER):**
- `GET /api/reports/orders` - Отчет по заказам
- `GET /api/reports/salary` - Отчет по зарплатам

**Экспорт:**
- `GET /api/export/orders` - Экспорт заказов

**Резервное копирование (ADMIN):**
- `GET /api/backup` - Список бэкапов

**Логи (ADMIN):**
- `GET /api/logs` - Список логов

**Продукты (ADMIN):**
- `GET /api/products` - Список продуктов
- `GET /api/products/stats` - Статистика продуктов
- `GET /api/products/categories` - Категории продуктов

### Результаты тестирования

Скрипт создает JSON отчет с результатами:

```
test-results-YYYYMMDD-HHMMSS.json
```

Отчет содержит:
- Общую статистику (всего, пройдено, провалено, пропущено)
- Детальную информацию по каждому тесту
- Время выполнения каждого запроса
- Коды статусов ответов

### Интерпретация результатов

- ✅ **PASS** (200-299) - Тест пройден успешно
- ⚠️ **AUTH** (401/403) - Требуется аутентификация (ожидаемо для некоторых эндпоинтов)
- ❌ **FAIL** (400-499) - Ошибка клиента
- ❌ **ERROR** (500+) - Ошибка сервера

## 🔑 Тестовые учетные записи

После выполнения `seed:comprehensive` доступны следующие учетные записи:

### Администратор
- **Email:** `admin@coffee.com`
- **Password:** `admin123`
- **Роль:** ADMIN
- **Доступ:** Полный доступ ко всем функциям

### Менеджер
- **Email:** `manager@coffee.com`
- **Password:** `manager123`
- **Роль:** MANAGER
- **Доступ:** Управление заказами, организациями, статистика

### Менеджер 2
- **Email:** `manager2@coffee.com`
- **Password:** `manager123`
- **Роль:** MANAGER

### Инженер 1 (Иван Петров)
- **Email:** `engineer1@coffee.com`
- **Password:** `engineer123`
- **Роль:** USER
- **Тип:** Штатный
- **Доступ:** Просмотр назначенных заказов, выполнение работ

### Инженер 2 (Сергей Сидоров)
- **Email:** `engineer2@coffee.com`
- **Password:** `engineer123`
- **Роль:** USER
- **Тип:** Наемный
- **Доступ:** Просмотр назначенных заказов, выполнение работ

### Инженер 3 (Алексей Иванов)
- **Email:** `engineer3@coffee.com`
- **Password:** `engineer123`
- **Роль:** USER
- **Тип:** Штатный
- **Доступ:** Просмотр назначенных заказов, выполнение работ

## 📊 Статистика после seed

После выполнения `seed:comprehensive` в базе данных будет:

- **6 пользователей** (1 админ, 2 менеджера, 3 инженера)
- **6 организаций** с разными тарифами
- **3 инженера** с разными типами
- **6 заказов** с разными статусами
- **Рабочие сессии** для заказов в работе
- **Тарифы** инженер-организация
- **Выплаты** (авансы и зарплаты)
- **4 продукта**
- **Настройки** системы

## 🔄 Сброс базы данных

Для полного сброса и повторного заполнения:

```bash
# Удалить базу данных (SQLite)
rm backend/database.sqlite

# Запустить расширенный seed
cd backend
npm run seed:comprehensive
```

## 💡 Советы

1. **Перед тестированием:** Убедитесь, что сервер запущен
2. **После seed:** Проверьте логи на наличие ошибок
3. **При тестировании:** Обратите внимание на эндпоинты, требующие аутентификации
4. **Для отладки:** Используйте JSON отчет для анализа проблемных эндпоинтов

## 🐛 Устранение неполадок

### Ошибка аутентификации

Если тесты не проходят из-за аутентификации:
1. Убедитесь, что пользователи созданы: `npm run seed:comprehensive`
2. Проверьте, что сервер запущен
3. Проверьте правильность URL в скрипте

### Ошибки при создании данных

Если seed падает с ошибками:
1. Проверьте, что база данных доступна
2. Убедитесь, что все миграции применены
3. Проверьте логи на наличие конфликтов (дубликаты email и т.д.)

### Проблемы с правами доступа

Если скрипт не запускается:
```bash
chmod +x test-all-endpoints-comprehensive.sh
```



